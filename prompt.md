# Quick Change

In the promnpt system append to the command sprint-commit, I want to add dynamically the current git status result. The orchestrator script, when spawning this command, need to add in its prompt system the content of git status inside XML. Inside the injected XML, an instruction to mention that it is the result of git status executed right before spawning this agent.

# Autonomous development

Use sub agent to run appropriate BMAD method command to develop this feature. Be 100% autonomous take all decision with the commands.